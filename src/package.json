{
  "name": "src",
  "module": "index.ts",
  "devDependencies": {
    "@aws-sdk/client-dynamodb": "^3.978.0",
    "@aws-sdk/lib-dynamodb": "^3.978.0",
    "@types/aws-lambda": "^8.10.160",
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "private": true,
  "scripts": {
    "build": "bun build index.ts --outfile ../terraform/index.js --target node --format cjs --external '@aws-sdk/*'"
  },
  "type": "module"
}
